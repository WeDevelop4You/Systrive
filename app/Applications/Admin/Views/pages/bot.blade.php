<!DOCTYPE html>
<html lang="{{ str_replace('_', '-', app()->getLocale()) }}">
    <head>
        <title>{{ config('APP_NAME') }}</title>

        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <body style="margin: 0">
        <div style="width: 100vw">
            <h1 style="margin: 0 auto; width: max-content">Sorry but Bots and Scrapers are not allowed</h1>
        </div>
    </body>
</html>
