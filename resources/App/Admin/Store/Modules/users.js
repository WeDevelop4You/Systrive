// import Vue from 'vue';

// const app = Vue.prototype

export default {
    namespaced: true,

    actions: {
        edit(_, route) {
            console.log(route)
        },

        update(_, route) {
            console.log(route)
        }
    },
}
